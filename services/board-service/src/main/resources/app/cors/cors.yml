app:
  security:
    cors:
      exposed-headers: "*"
      allowed:
        methods: "*"
        headers: "*"
        origins:
          - http://localhost:3030
        credentials: true


        # 프로파일 잘 안 되면 아래처럼 목록 추가해서 사용
