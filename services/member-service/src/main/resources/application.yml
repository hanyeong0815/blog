server:
  port: 8080

spring:
  config:
    import:
      - app/cors/cors.yml
      - app/time/time.yml # classpath:/app/time/time.yml
      - app/jwt/jwt.yml
      - app/password/password-encoder.yml
      - app/url/url.yml

  datasource:
    url: ${MEMBER_URL:jdbc:postgresql://rdb-blog-member.cl002464cek4.ap-northeast-2.rds.amazonaws.com:5432/member}
    username: ${MEMBER_USER:blog_amdin}
    password: ${POSTGRES_PASSWORD:rhehdgur23!}
    driver-class-name: org.postgresql.Driver

    hikari:
      connection-timeout: 10000
      idle-timeout: 10000
      validation-timeout: 10000
      max-lifetime: 580000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:/db/migration # 기본값

  data:
    redis:
      host: 3.35.139.172
      port: 6379
